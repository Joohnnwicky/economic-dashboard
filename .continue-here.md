---
context: default
phase: null
task: 7
total_tasks: 11
status: paused
last_updated: 2026-06-10T18:00:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [x] CONSTRAINT: jin10数据排列方向 — AkShare返回的jin10格式数据是 index 0=最早, index -1=最新，解析器必须从DataFrame末尾取数据，取前N条会返回90年代旧数据

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| jin10取前N行 | _parse_jin10_series取df前36行得到90年代数据而非2025年数据 | blocking | 必须用 `start = max(0, total - max_points)` 从末尾取 |
| Windows进程杀不掉 | bash的kill -9无法杀Windows uvicorn进程，导致代码修改后旧进程仍返回旧数据 | advisory | 需要用户手动Ctrl+C关闭后重启 |

<current_state>
基于Barclays中国展望报告的指标扩展已完成7/11个任务。jin10数据排序bug已修复（Python直接调用验证PMI返回2025年数据），但后端uvicorn进程未重启，API仍返回旧数据。用户反馈了4个新问题待修复：金价无数据、油价要更实时、汇率加载失败、虚拟币要加币安前10。
</current_state>

<completed_work>

- ✅ 后端：ChinaMacroCache(24h TTL) + _parse_jin10_series通用解析器 + 11个新AkShare函数
- ✅ 后端：失业率爬虫(unemployment_service.py) + 静态JSON双保险
- ✅ 后端：修复jin10数据排序bug（从末尾取最新数据）
- ✅ 前端：扩展TypeScript类型(ChinaPMIGroup/TradeGroup/CreditGroup/OtherGroup)
- ✅ 前端：扩展useChinaMacroBackend hook
- ✅ 前端：新建ChinaPMIPanel（4个PMI+荣枯线）
- ✅ 前端：新建ChinaTradePanel（双Y轴图表）
- ✅ 前端：新建ChinaCreditPanel（M2/社融/新增贷款）
- ✅ 前端：扩展ChinaMacroPanel（+工业增加值/失业率/外汇储备）
- ✅ 前端：扩展PBOCRatePanel（+7天OMO逆回购利率）
- ✅ 前端：Dashboard.tsx集成3个新面板
- ✅ 静态数据：china-unemployment.json + pboc-rates.json(OMO数据)
</completed_work>

<remaining_work>

- ❌ 修复国际金价无数据
- ❌ 国际油价换更实时API
- ❌ 修复主要汇率加载失败
- ❌ 虚拟币增加币安交易量前10币种
</remaining_work>

<decisions_made>

- AkShare作为13/14个中国指标的主要数据源（免费无Key）
- 失业率用爬虫+静态JSON双保险（NBS 403, AkShare失效）
- 7天OMO利率手动维护在pboc-rates.json（AkShare无此数据）
- jin10数据从DataFrame末尾取（index -1 = 最新）
</decisions_made>

<blockers>
- 后端uvicorn进程未重启：用户需手动Ctrl+C关闭后 `cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8000`
</blockers>

## Required Reading (in order)
1. `backend/services/china_macro_service.py` — _parse_jin10_series的数据排序逻辑
2. `src/components/indicators/ChinaPMIPanel.tsx` — PMI面板参考实现
3. `src/api/china-macro-backend.ts` — 扩展后的TypeScript类型定义

## Infrastructure State
- 后端：http://localhost:8000 运行中（但代码未更新，需重启）
- 前端：http://localhost:5173 运行中
- 所有修改未提交git

<context>
Barclays报告驱动的中国宏观经济指标扩展。核心数据排序bug已修复并验证。4个用户反馈的新问题待处理。
</context>

<next_action>
1. 用户手动重启后端验证数据修复
2. 修复金价/油价/汇率/币安前10等问题
</next_action>
