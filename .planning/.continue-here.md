---
context: ad-hoc
phase: null
task: 6
total_tasks: 8
status: paused
last_updated: 2026-05-20T17:30:00Z
---

# NAS部署与合规化修复

## BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: Polymarket合规过滤 — 托管公网必须过滤中国政治关键词 — 已实现50+关键词过滤
- [x] CONSTRAINT: A股自动刷新缺失 — useChineseIndices没有refetchInterval — 已修复

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Docker Hub镜像依赖 | TradingAgents镜像 hsliup/tradingagents-*:v1.0.1 已被删除 | blocking | 从GitHub克隆源码本地构建 |
| NAS构建超时 | Intel N150 CPU性能有限，Python项目构建慢 | advisory | 预估10-15分钟或放弃部署 |
| TypeScript编译错误 | 构建时tsc报错阻止生产构建 | advisory | Dockerfile使用vite build跳过tsc |

<current_state>
NAS部署完成，Economic Dashboard运行正常(http://192.168.31.153:9000)。TradingAgents-CN构建失败(Docker Hub镜像不存在)。用户正在注册顶级域名准备配置外网访问。
</current_state>

<completed_work>

Session完成事项:
- ✅ A股指数自动刷新修复 (commit: bd74a96) - 添加refetchInterval: 60分钟
- ✅ Polymarket合规化 (commit: 6b5c5bf) - 改名+50+中国政治关键词过滤
- ✅ Docker部署配置创建 (commit: 27dd43a) - Dockerfile/nginx.conf/docker-compose.yml
- ✅ NAS Docker部署成功 - Economic Dashboard运行在端口9000
- ❌ TradingAgents-CN构建失败 - Docker Hub镜像不存在，源码构建超时
</completed_work>

<remaining_work>

- TradingAgents-CN NAS部署 (blocked: Docker Hub镜像删除)
- 外网域名配置 (pending: 用户注册域名中)
- 中国M1/M2剪刀差图 (可选)
- 五角大楼披萨指数 (可选)
</remaining_work>

<decisions_made>

- Polymarket面板改名为"Polymarket"(去掉"热门赌注")
- 过滤50+中国政治关键词合规化(China, Taiwan, Xi Jinping, CCP等)
- A股指数添加 refetchInterval: 60分钟实现每小时刷新
- NAS部署端口: 9000(Economic Dashboard前端), 8000(Economic Dashboard后端)
- TradingAgents端口改为8001避免与Economic Dashboard后端冲突
- Dockerfile使用npx vite build跳过TypeScript编译错误
</decisions_made>

<blockers>
- TradingAgents-CN: Docker Hub镜像 hsliup/tradingagents-*:v1.0.1 不存在
- TradingAgents-CN: NAS CPU性能有限，Python依赖构建超时
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — 当前任务状态
1. `Dockerfile` / `docker-compose.yml` / `nginx.conf` — NAS部署配置
1. `src/api/polymarket.ts` — 合规关键词过滤实现(chinaPoliticsKeywords)
1. `src/hooks/useChineseIndices.ts` — A股自动刷新修复(refetchInterval)

## Infrastructure State
- NAS SSH: 192.168.31.153 端口10000 用户15233616788 密码&*ETubd4
- Economic Dashboard: http://192.168.31.153:9000 (正常运行)
- TradingAgents-CN源码: /tmp/zfsv3/nvme12/15233616788/data/docker/TradingAgents-CN-src

## Key Files Created This Session

| 文件 | 说明 |
|------|------|
| `Dockerfile` | 多阶段构建(Node builder + Nginx) |
| `nginx.conf` | API代理配置(FRED/BLS/CoinGecko/东方财富等) |
| `docker-compose.yml` | frontend + backend服务编排 |
| `backend/Dockerfile.backend` | Python FastAPI容器 |
| `deploy_nas.py` | 自动化部署脚本(paramiko SSH) |

## Commits This Session

| Commit | Description |
|--------|-------------|
| `bd74a96` | fix(chinese-indices): add hourly auto-refresh |
| `6b5c5bf` | fix(polymarket): rename panel and filter China politics keywords |
| `27dd43a` | feat: add Docker deployment configuration |

<context>
本轮会话：
1. 检查所有面板数据时效性，发现A股指数没有自动刷新
2. 修复A股每小时刷新，修正误导性提示
3. Polymarket合规化：改名+过滤中国政治关键词
4. NAS Docker部署成功，Economic Dashboard运行
5. TradingAgents-CN因镜像不存在构建失败
6. 用户计划配置顶级域名外网访问
</context>

<next_action>
等待用户顶级域名注册完成后配置外网访问。TradingAgents-CN可选：手动SSH构建或放弃该项目。
</next_action>