---
context: phase
phase: 03-professional-experience
task: 3
total_tasks: 8
status: paused
last_updated: 2026-05-19T07:35:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: BLS Series IDs — Food/Energy CPI series IDs were incorrect (CUSR0000SEF/CUSR0000SEB), fixed to correct IDs (CUSR0000SAF/CUSR0000SA0E) — Always verify series IDs via curl before assuming BLS data availability

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Cascade Loading Block | OverlayPanel blocked on ANY loading source, preventing useful partial data display | advisory | Changed to partial-data-display: show chart if >=2 indicators available, only show failure message when ALL sources failed and not loading |
| Stale Symbol Mapping | ETF names mapping function only had old index symbols (DJI/NASDAQ/SPX), not new ETF symbols (DIA/QQQ/SPY) | advisory | Updated getIndexName() to include both ETF and index symbol mappings |

<current_state>
UAT testing for Phase 03 (Professional Experience). Test 3 (WebSocket Reconnection) was blocked by API data issues, now unblocked after fixes. User reports CPI consumer price index panel still showing loading spinner. All API endpoints verified working via curl - likely React Query cache issue in browser.
</current_state>

<completed_work>

Session fixes (8 commits):
- 6b840c4: WebSocket stale closure bug fix
- 4b7801d: CORS proxy + WebSocket ping filter
- fb1b668: A股指数面板 added to Dashboard
- 106b285: Alpha Vantage ETF symbols (DIA/QQQ/SPY) + Eastmoney endpoint fix
- 781fb6b: EmploymentSubMetricsPanel + InflationSubMetricsPanel + PBOCRatePanel added
- 2b94265: BLS series IDs corrected + OverlayPanel cascade blocking fix
- 719b648: ETF symbol names mapping update
- 4f28351: UAT session notes update

UAT Test Progress:
- Test 1: WebSocket Connection Indicator - PASS
- Test 2: Real-Time Crypto Prices - PARTIAL (CoinGecko rate limited, Binance WS works)
- Tests 4-8: Pending (dataZoom, FOMC markers)
</completed_work>

<remaining_work>

- Test 3: WebSocket Reconnection - needs verification after user clears cache
- Test 4: dataZoom Slider on Charts - pending
- Test 5: dataZoom Zoom Functionality - pending
- Test 6: FOMC Markers on Fed Rate Chart - pending
- Test 7: FOMC Marker Colors - pending
- Test 8: FOMC Marker Tooltip - pending
</remaining_work>

<decisions_made>

- Alpha Vantage ETF proxies chosen because TIME_SERIES_DAILY doesn't support index symbols
- BLS series IDs verified via curl before fixing code
- OverlayPanel partial-data-display chosen over cascade-blocking for better UX
- ETF names mapped for display (DIA→道琼斯指数, QQQ→纳斯达克指数, SPY→标普500指数)
</decisions_made>

<blockers>
- CPI consumer price index panel loading spinner: API verified working via curl, likely React Query browser cache issue. User needs to clear browser storage (DevTools → Application → Clear site data) and hard refresh (Ctrl+Shift+R).
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-professional-experience/03-UAT.md` — UAT test tracking, all fixes documented
1. `src/hooks/useBlsData.ts` — CPI data fetching hook
1. `src/api/bls.ts` — BLS API client, verify series ID constants
1. `src/components/layout/OverlayPanel.tsx` — Partial-data-display logic

## Infrastructure State
- Dev server running: `npm run dev` on port 5173
- Browser: Chrome with React DevTools
- Console shows: All API calls executing (BLS 4 calls, Alpha Vantage 3 calls, EastMoney 1 call)
- No red errors in console

<context>
UAT testing session. API fixes verified working via curl. CPI panel stuck in loading state despite API returning correct data. Console shows no errors. Most likely React Query caching stale loading state or rate-limiter internal state blocking. User should clear browser storage before continuing diagnosis.
</context>

<next_action>
Resume: Ask user to clear browser storage (DevTools → Application → Clear site data → Clear) and hard refresh (Ctrl+Shift+R). Then:
1. Check CPI panel - should now display data
2. Continue UAT Test 3 (WebSocket Reconnection)
3. If still spinning, check Network tab BLS POST response structure vs expected format in `src/api/bls.ts`
</next_action>