---
phase: 03-professional-experience
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/constants/api.ts, src/api/eastmoney.ts, vite.config.ts]
autonomous: true
gap_closure: true
requirements: [IND-13, IND-14, IND-15, IND-10, IND-11]
user_setup: []

must_haves:
  truths:
    - "美股大盘指数面板显示道琼斯、纳斯达克、标普500数据"
    - "A股指数面板显示上证指数、深证成指、沪深300数据"
  artifacts:
    - path: "src/constants/api.ts"
      provides: "Alpha Vantage symbol configuration"
      contains: "ALPHA_VANTAGE_SYMBOLS"
    - path: "src/api/eastmoney.ts"
      provides: "A-share indices data fetch"
      exports: ["getChineseIndices"]
  key_links:
    - from: "src/constants/api.ts"
      to: "src/api/alphavantage.ts"
      via: "ALPHA_VANTAGE_SYMBOLS import"
      pattern: "ALPHA_VANTAGE_SYMBOLS"
    - from: "src/api/eastmoney.ts"
      to: "https://push2.eastmoney.com/api/qt/ulist.np"
      via: "axios GET request"
      pattern: "EASTMONEY_INDEX_URL"
---

<objective>
Fix critical API failures blocking 美股大盘指数面板 and A股指数面板.

Purpose: Both US indices and A-share indices panels are broken due to incorrect API configurations. Alpha Vantage symbols need to be changed to ETF proxies, and Eastmoney endpoint needs verification/update.

Output: Working US indices (DIA, QQQ, SPY) and A-share indices (上证指数, 深证成指, 创业板指) data fetching.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-professional-experience/03-UAT.md

# Current Alpha Vantage symbols (broken)
@src/constants/api.ts

# Eastmoney API implementation
@src/api/eastmoney.ts

# Chinese indices configuration
@src/constants/chinese-indices.ts

# Vite proxy configuration
@vite.config.ts
</context>

<interfaces>
<!-- Key contracts the executor needs -->

From src/constants/api.ts:
```typescript
export const ALPHA_VANTAGE_SYMBOLS = {
  DOW_JONES: 'DJI',           // Dow Jones Industrial Average
  NASDAQ: 'NASDAQ',           // NASDAQ Composite
  SP500: 'SPX',               // S&P 500
} as const;
```

From src/api/eastmoney.ts:
```typescript
export async function getChineseIndices(): Promise<NormalizedIndicator[]>
```

From src/constants/chinese-indices.ts:
```typescript
export const CHINESE_INDEX_CODES = {
  SSE_COMPOSITE: '1.000001',     // 上证指数
  SZSE_COMPONENT: '0.399001',    // 深证成指
  CHINEXT: '0.399006',           // 创业板指
} as const;

export const EASTMONEY_INDEX_FIELDS = 'f2,f3,f4,f12,f14,f15,f16,f17,f18' as const;
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix Alpha Vantage symbols to ETF proxies</name>
  <files>src/constants/api.ts</files>
  <action>
    Per UAT Gap 1 diagnosis: Alpha Vantage TIME_SERIES_DAILY API does not support index symbols (DJI, NASDAQ, SPX). Verified via curl tests: IBM and SPY return data, DJI returns empty {}.

    Change ALPHA_VANTAGE_SYMBOLS from index symbols to ETF proxies:
    - DOW_JONES: 'DJI' → 'DIA' (Dow Jones ETF)
    - NASDAQ: 'NASDAQ' → 'QQQ' (Nasdaq-100 ETF)
    - SP500: 'SPX' → 'SPY' (S&P 500 ETF)

    Also update comment to reflect the change and explain rationale:
    "// Alpha Vantage TIME_SERIES_DAILY doesn't support index symbols directly."
    "// Using ETF proxies: DIA (Dow), QQQ (Nasdaq-100), SPY (S&P 500)"

    Do NOT modify any other constants (RATE_LIMITS, BLS_SERIES, FRED constants remain unchanged).
  </action>
  <verify>
    <automated>npx vitest run src/api/__tests__/alphavantage.test.tsx --grep="getUSIndices"</automated>
    Manual verification: curl test with new symbols should return data.
    Command: curl "https://www.alphavantage.co/query?function=TIME_SERIES_DAILY&symbol=SPY&apikey=demo" | head -20
  </verify>
  <done>
    ALPHA_VANTAGE_SYMBOLS uses ETF symbols (DIA, QQQ, SPY).
    USIndicesPanel renders with real price data for all three indices.
    Alpha Vantage API calls return non-empty response data.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix Eastmoney API endpoint</name>
  <files>src/api/eastmoney.ts, vite.config.ts</files>
  <action>
    Per UAT Gap 2 diagnosis: push2.eastmoney.com/api/qt/ulist.np endpoint not responding. Vite proxy already configured with Referer and User-Agent headers.

    Research alternative Eastmoney endpoints for A-share indices. Try these known working patterns:

    1. **Quote API (qt/stock/get):**
       Endpoint: `https://push2.eastmoney.com/api/qt/stock/get`
       Parameters: `secid=${secid}&fields=${fields}`
       Try with individual secids: 1.000001, 0.399001, 0.399006

    2. **ulist.np alternative format:**
       Current: `ulist.np?fltt=2&secids=${secids}&fields=${fields}`
       Try: `ulist.np?fltt=2&invt=2&secids=${secids}&fields=${fields}&cb=&ut=b2884a393a59ad6400f92e6b8e9`
       (Adding cb= and ut= parameters that some Eastmoney endpoints require)

    3. **If above fail, use Sina Finance as backup:**
       Endpoint: `https://hq.sinajs.cn/list=${codes}`
       Codes: sh000001, sz399001, sz399006
       Response format: comma-separated values string
       Parse: var hq_str_sh000001="上证指数,3050.12,..."

    Testing approach:
    - Use curl via Vite proxy to test endpoints: curl "http://localhost:5173/api/eastmoney/qt/stock/get?secid=1.000001&fields=f2,f3,f4,f12,f14"
    - If endpoint works, update EASTMONEY_INDEX_URL in eastmoney.ts
    - If Eastmoney fails completely, create sina.ts API client and update hook

    Keep rate limiting configuration unchanged (60-min cache, 500/day limit).

    DO NOT change CHINESE_INDEX_CODES or EASTMONEY_INDEX_FIELDS - these are correct.
  </action>
  <verify>
    <automated>npx vitest run src/api/__tests__/eastmoney.test.tsx</automated>
    Manual: curl test returns JSON with data.diff array containing index info.
  </verify>
  <done>
    ChineseIndicesPanel renders with 上证指数, 深证成指, 创业板指 real-time data.
    Eastmoney/Sina API returns valid JSON with price fields (f2/f3/f4 or equivalent).
    API call via Vite proxy succeeds without CORS errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Alpha Vantage API | Untrusted input via proxy, API key in env |
| Client → Eastmoney/Sina API | Untrusted external API, no auth required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Spoofing | Alpha Vantage symbols | accept | ETF symbols are public data, no spoofing risk |
| T-03-04-02 | Tampering | Eastmoney response | mitigate | Validate response.data.diff exists before processing |
| T-03-04-03 | Denial of Service | Rate limiter | mitigate | 60-min cache prevents quota exhaustion |
</threat_model>

<verification>
After completing tasks:

1. **US Indices verification:**
   - Start dev server: `npm run dev`
   - Navigate to dashboard
   - Verify 美股大盘指数面板 shows DIA, QQQ, SPY with real prices
   - Check console for Alpha Vantage API errors (should be none)

2. **A-share indices verification:**
   - Verify A股指数面板 shows 上证指数, 深证成指, 创业板指
   - Check console for Eastmoney/Sina API errors (should be none)

3. **Cross-market overlay:**
   - Verify OverlayPanel loads without blocking spinner
   - All indicators should appear in comparison dropdown
</verification>

<success_criteria>
- [ ] ALPHA_VANTAGE_SYMBOLS changed to ETF symbols (DIA, QQQ, SPY)
- [ ] USIndicesPanel displays real-time price data for all three US indices
- [ ] ChineseIndicesPanel displays 上证指数, 深证成指, 创业板指 data
- [ ] No API errors in browser console for Alpha Vantage or Eastmoney
- [ ] OverlayPanel comparison chart loads successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-professional-experience/03-04-SUMMARY.md`
</output>