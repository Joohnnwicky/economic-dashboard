---
phase: 03-professional-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/charts/LineChart.tsx
  - src/components/charts/MultiSeriesChart.tsx
  - src/utils/formatters.ts
autonomous: true
requirements: [CHART-04, CHART-08]
user_setup: []

must_haves:
  truths:
    - "User can drag dataZoom slider handles to zoom into specific time periods"
    - - "dataZoom slider appears at bottom of all time-series charts"
    - "Initial zoom shows all data (start: 0, end: 100)"
    - "Slider uses dark theme colors matching dashboard style"
    - "Zooming filters data points (out-of-range points not rendered)"
    - "User can reset zoom by moving slider handles back to 0/100"
  artifacts:
    - path: "src/components/charts/LineChart.tsx"
      provides: "Line chart with dataZoom slider"
      contains: "dataZoom: [{type: 'slider', ...}]"
    - path: "src/components/charts/MultiSeriesChart.tsx"
      provides: "Multi-series chart with dataZoom slider"
      contains: "dataZoom: [{type: 'slider', ...}]"
  key_links:
    - from: "LineChart.tsx dataZoom"
      to: "DARK_THEME colors"
      via: "backgroundColor: DARK_THEME.panel, textStyle: {color: DARK_THEME.textMuted}"
      pattern: "dataZoom.*DARK_THEME"
---

<objective>
Add ECharts dataZoom slider to all time-series charts, enabling users to zoom in/out on specific time periods for detailed examination.

Purpose: Deliver professional-level chart interaction per project vision "专业级图表交互", allowing users to examine specific time periods (e.g., Fed rate changes during 2008 crisis).
Output: Enhanced LineChart and MultiSeriesChart with dataZoom configuration, DARK_THEME styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing patterns to enhance
@src/components/charts/LineChart.tsx — Base chart component to add dataZoom
@src/components/charts/MultiSeriesChart.tsx — Multi-series overlay chart to add dataZoom
@src/constants/colors.ts — DARK_THEME colors for styling

# Decisions from CONTEXT.md (MUST follow)
D-07: Use ECharts dataZoom type: 'slider' positioned at chart bottom
D-08: Initial zoom shows all data (start: 0, end: 100)
D-09: Slider styling uses existing DARK_THEME colors (background, gridLine, text)

# ECharts dataZoom configuration (from RESEARCH.md)
dataZoom: [{
  type: 'slider',
  show: true,
  xAxisIndex: 0,
  start: 0,
  end: 100,
  height: 20,
  bottom: 10,
  backgroundColor: DARK_THEME.panel,
  dataBackground: {
    lineStyle: { color: DARK_THEME.accent[0] },
    areaStyle: { color: DARK_THEME.accent[0], opacity: 0.3 }
  },
  fillerColor: 'rgba(88, 166, 255, 0.2)',
  borderColor: DARK_THEME.gridLine,
  handleStyle: {
    color: DARK_THEME.accent[0],
    borderColor: DARK_THEME.accent[0]
  },
  textStyle: { color: DARK_THEME.textMuted }
}]
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dataZoom to LineChart component</name>
  <files>src/components/charts/LineChart.tsx</files>
  <read_first>
    - J:\经济指标看板\src\components\charts\LineChart.tsx — Current LineChart implementation to enhance
    - J:\经济指标看板\src\constants\colors.ts — DARK_THEME color definitions for styling
    - J:\经济指标看板\.planning\phases\03-professional-experience\03-CONTEXT.md — D-07 to D-09 decisions
    - J:\经济指标看板\.planning\phases\03-professional-experience\03-RESEARCH.md — ECharts dataZoom configuration example
  </read_first>
  <behavior>
    - Test 1: LineChart renders dataZoom slider at bottom
    - Test 2: dataZoom type is 'slider' (D-07)
    - Test 3: Initial zoom is start: 0, end: 100 (D-08)
    - Test 4: Slider backgroundColor is DARK_THEME.panel
    - Test 5: Slider textStyle color is DARK_THEME.textMuted
    - Test 6: handleStyle color is DARK_THEME.accent[0]
    - Test 7: Zooming changes visible data range
  </behavior>
  <action>
Enhance LineChart.tsx with dataZoom configuration following D-07, D-08, D-09:

1. Add dataZoom array to ECharts option object (insert after tooltip):
   ```typescript
   dataZoom: [
     {
       type: 'slider',
       show: true,
       xAxisIndex: 0,
       start: 0,
       end: 100,
       height: 20,
       bottom: 10,
       backgroundColor: DARK_THEME.panel,
       dataBackground: {
         lineStyle: { color: DARK_THEME.accent[0] },
         areaStyle: { color: DARK_THEME.accent[0], opacity: 0.3 }
       },
       fillerColor: 'rgba(88, 166, 255, 0.2)',
       borderColor: DARK_THEME.gridLine,
       handleStyle: {
         color: DARK_THEME.accent[0],
         borderColor: DARK_THEME.accent[0]
       },
       textStyle: { color: DARK_THEME.textMuted }
     }
   ]
   ```

2. Adjust grid.bottom to accommodate slider (increase from '15%' to '20%'):
   - Original: grid: { left: '10%', right: '5%', top: '10%', bottom: '15%' }
   - Updated: grid: { left: '10%', right: '5%', top: '10%', bottom: '20%' }

3. Ensure filterMode is default ('filter') — only renders visible points (performance optimization).

4. Do NOT add dataZoom type: 'inside' (mouse wheel zoom) — users need visible slider for precise control (per D-07).

Implementation preserves existing functionality (tooltip, series, axes) while adding dataZoom.
  </action>
  <verify>
    <automated>npm test -- --filter=LineChart</automated>
  </verify>
  <acceptance_criteria>
    - LineChart.tsx contains dataZoom array in option object
    - dataZoom type: 'slider' (D-07)
    - dataZoom start: 0, end: 100 (D-08)
    - dataZoom backgroundColor: DARK_THEME.panel (D-09)
    - dataZoom textStyle: { color: DARK_THEME.textMuted } (D-09)
    - dataZoom handleStyle: { color: DARK_THEME.accent[0] }
    - grid.bottom adjusted to '20%' to accommodate slider
    - Chart renders slider at bottom (visual verification)
  </acceptance_criteria>
  <done>LineChart displays dataZoom slider with dark theme styling, enabling zoom into specific time periods</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dataZoom to MultiSeriesChart component</name>
  <files>src/components/charts/MultiSeriesChart.tsx</files>
  <read_first>
    - J:\经济指标看板\src\components\charts\MultiSeriesChart.tsx — Current multi-series chart to enhance
    - J:\经济指标看板\src\constants\colors.ts — DARK_THEME colors
    - J:\经济指标看板\.planning\phases\03-professional-experience\03-CONTEXT.md — D-07 to D-09 decisions
  </read_first>
  <behavior>
    - Test 1: MultiSeriesChart renders dataZoom slider
    - Test 2: dataZoom applies to both series (xAxisIndex: 0)
    - Test 3: Slider styling matches LineChart (D-09)
    - Test 4: Zooming affects both left and right axis series simultaneously
  </behavior>
  <action>
Enhance MultiSeriesChart.tsx with same dataZoom configuration as LineChart:

1. Add dataZoom array after tooltip in option object:
   - Same configuration as LineChart (copy exact values)
   - type: 'slider', start: 0, end: 100
   - DARK_THEME styling (backgroundColor, textStyle, handleStyle)

2. Adjust grid.bottom to '20%' for slider space.

3. Ensure dataZoom applies to xAxisIndex: 0 (both series use same xAxis).

4. Verify both left and right axis series zoom together (same xAxis index).

Implementation mirrors LineChart enhancement for consistency across all charts.
  </action>
  <verify>
    <automated>npm test -- --filter=MultiSeriesChart</automated>
  </verify>
  <acceptance_criteria>
    - MultiSeriesChart.tsx contains dataZoom array
    - dataZoom configuration matches LineChart (type: 'slider', start: 0, end: 100)
    - DARK_THEME styling applied (backgroundColor, textStyle)
    - grid.bottom adjusted to '20%'
    - Both left and right axis series zoom together
  </acceptance_criteria>
  <done>MultiSeriesChart displays dataZoom slider, enabling cross-market comparison zoom</done>
</task>

<task type="auto">
  <name>Task 3: Update formatters for zoom context</name>
  <files>src/utils/formatters.ts</files>
  <read_first>
    - J:\经济指标看板\src\utils\formatters.ts — Existing formatter utilities
    - J:\经济指标看板\src\components\charts\LineChart.tsx — Formatter usage in tooltip
  </read_first>
  <action>
Review formatters.ts to ensure zoom context compatibility:

1. formatChartDate function already exists — no changes needed (handles timeRange parameter).

2. formatPercentage function already exists — no changes needed.

3. No new formatter functions needed for dataZoom (ECharts handles zoom labels internally).

This task is verification-only: ensure existing formatters work correctly when dataZoom changes visible data range.

Manual verification: zoom into specific period, verify tooltip dates and values display correctly.
  </action>
  <verify>
    <automated>npm test -- --filter=formatters</automated>
  </verify>
  <acceptance_criteria>
    - formatChartDate works correctly when zoomed (tooltip shows correct date)
    - formatPercentage works correctly when zoomed (tooltip shows correct value)
    - No formatter changes needed (existing functions sufficient)
  </acceptance_criteria>
  <done>Formatters verified to work correctly with zoomed data context</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → dataZoom slider | UI interaction, trusted input (ECharts handles internally) |
| dataZoom → Chart renderer | Internal ECharts configuration, trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | dataZoom on large datasets | mitigate | ECharts filterMode: 'filter' removes out-of-range points from rendering (RESEARCH.md Pitfall 4) |
| T-03-06 | Spoofing | dataZoom slider display | accept | Slider is UI-only, no security impact |
</threat_model>

<verification>
1. Start development server: npm run dev
2. Open dashboard, navigate to Fed Rate panel
3. Verify dataZoom slider appears at bottom of chart
4. Drag left handle to 50%, verify chart shows only right half of data
5. Drag both handles back to 0/100, verify all data visible again
6. Test on Crypto panel (MiniChart may not need dataZoom)
7. Test on MultiSeriesChart (OverlayComparisonChart)
8. Verify slider styling matches dark theme (dark background, blue handles)
</verification>

<success_criteria>
- dataZoom slider appears on all time-series charts (LineChart, MultiSeriesChart)
- Slider positioned at chart bottom (D-07)
- Initial zoom shows all data (D-08)
- Slider styling uses DARK_THEME colors (D-09)
- Users can zoom into specific time periods
- Zoom resets when handles moved to 0/100
- All automated tests pass (LineChart, MultiSeriesChart, formatters)
</success_criteria>

<output>
After completion, create `.planning/phases/03-professional-experience/03-02-SUMMARY.md`
</output>